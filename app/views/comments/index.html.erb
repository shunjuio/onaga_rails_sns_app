<h3>Comments:</h3>
<%= link_to "New Comment", new_comment_pat %>

<% if post.comments.any? %>
  <ul>
    <% post.comments.each do |comment| %>
      <li><%= comment.content %> - <%= comment.user.name if comment.user %></li>
    <% end %>
  </ul>
<% else %>
  <p>No comments yet.</p>
<% end %>
